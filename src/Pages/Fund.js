export default function Fund({Donor,Amount,Date}){
    return(<div>
        <div>
            <h1>Donor:{Donor}</h1>
            <h1>Amount:{Amount}</h1>
            <h2>Date:{Date}</h2>
        </div>
    </div>);
}